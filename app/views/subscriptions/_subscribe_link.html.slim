- if !current_user&.subscribed?(subscribable)
  = link_to 'Subscribe', subscriptions_path(id: subscribable.id), method: :post, remote: true
- else
  = link_to 'Unsubscribe', subscription_path(subscribable), method: :delete, remote: true
